#include <stdio.h>
int fact(int n);
int main() 
{
    int n,f;
	printf("Enter a number:");
    scanf("%d",&n);
    f=fact(n);
    printf("%d",f);
    return 0;
}
    int fact(int n)
    {
        if(n==1)
        return 1;
        if(n>0)
        return(n*fact(n-1));
    }